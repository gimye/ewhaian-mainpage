import styles from "./Border.module.css"

const Border = () => {

    return (
        <div className={styles.border}>
            <img src="logo/border.png" className={styles.image}></img>
        </div>
    )
}

export default Border;